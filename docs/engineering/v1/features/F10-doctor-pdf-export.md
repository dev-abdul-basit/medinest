# F10 · "Notes for Doctor" PDF Export

| | |
| --- | --- |
| Roadmap ref | T1-4 |
| Effort | 1 week |
| Risk | low–med |
| Schema change | no |
| New package | YES (`pdf` and `printing`) — Abdul approval required |
| Premium-gated | yes — premium feature |
| ASO signals moved | $$, ★ |
| Status | spec |

## 1. Why we're shipping it

The `01-store-listing.md` advertises journal + medicine history together as a feature designed around the doctor visit. We currently let users *write* journal notes but not *export* them. Adding a "Send to doctor" PDF export delivers the promise the listing makes. It's also the cleanest premium-gated feature we can ship without locking core reminder functionality — premium pays for "send PDF" with no reduction of free-tier ability to track.

## 2. What changes for the user

- In **Medicine History** screen: a new "Export PDF" button (top-right). Premium-gated.
- In **Appointment History** screen: same button.
- Tapping it opens a date-range picker (default: last 30 days), then the user selects which family-member's data and which sections to include (medicines / journal / appointments).
- Output: A4 PDF, MediNest-branded, includes a generated cover with patient name + date range, then chronological entries.
- Final action: `Share` (uses `share_plus` already in `pubspec.yaml`) or `Save to Files`.

## 3. What changes in the code

- **`pubspec.yaml`** — add `pdf: ^3.x` and `printing: ^5.x`. Abdul confirms.
- **NEW: `lib/services/pdf_export_service.dart`** *(new file)* — generates the PDF document. Returns `Uint8List` bytes.
- **NEW: `lib/ui/export_pdf/export_pdf_binding.dart`** *(new file)*
- **NEW: `lib/ui/export_pdf/export_pdf_logic.dart`** *(new file)*
- **NEW: `lib/ui/export_pdf/export_pdf_view.dart`** *(new file)* — the date-range and section picker UI.
- **`lib/routes/app_routes.dart`** — add `exportPdf` route.
- **`lib/routes/app_pages.dart`** — register.
- **`lib/ui/medicine_history_screen/medicine_history_screen_view.dart`** — add `Export PDF` action button. Premium gate.
- **`lib/ui/appointment_history_screen/appointment_history_screen_view.dart`** — same.
- **Locale files** — new keys.

## 4. Data model

No schema changes. PDF is generated from existing `medicine_history_table`, `journal_table`, `journal_history_table`, `family_member_table`.

## 5. Locale keys

```
'txtExportPdfTitle': "Export to PDF",
'txtExportPdfDateRange': "Date range",
'txtExportPdfLast30Days': "Last 30 days",
'txtExportPdfLast90Days': "Last 90 days",
'txtExportPdfLastYear': "Last year",
'txtExportPdfCustomRange': "Custom range",
'txtExportPdfSelectFamily': "Whose data?",
'txtExportPdfIncludeMeds': "Include medicine history",
'txtExportPdfIncludeJournal': "Include journal notes",
'txtExportPdfIncludeAppointments': "Include appointments",
'txtExportPdfGenerate': "Generate PDF",
'txtExportPdfSharing': "Generating PDF…",
'txtExportPdfSuccess': "PDF ready",
'txtExportPdfPremiumRequired': "Premium feature — upgrade to export.",
'txtExportPdfCoverTitle': "Medication summary",
'txtExportPdfCoverPatient': "Patient",
'txtExportPdfCoverPeriod': "Period",
'txtExportPdfFooter': "Generated by MediNest",
```

PDF section headers (rendered into the PDF, **also** localized via `.tr`):

```
'txtExportPdfSectionMeds': "Medications",
'txtExportPdfSectionJournal': "Journal entries",
'txtExportPdfSectionAppointments': "Appointments",
```

## 6. Routing

`exportPdf` route added.

## 7. Implementation steps (linear)

1. Confirm packages with Abdul. Run pub add (Abdul).
2. Add `Preference` access — none. Premium check uses existing `Preference.shared.getIsPurchase()`.
3. Add locale keys + replicate.
4. Create `PdfExportService`. Use the `pdf` package's `pw.Document()`. One method: `Future<Uint8List> generate({required DateTime from, required DateTime to, required FamilyMemberTable patient, required ExportSections sections})`. `ExportSections` is a small bool struct in the same file.
5. Page layout (in service):
   - Cover page: app logo (use `Assets.icons.icMedinest` or whatever exists in `lib/generated/assets.dart` — verify), patient name, period, generation date.
   - Sections: each as a heading + table of entries with date, medicine name, action (taken/skipped), dose, note (if present).
   - Footer on each page: `Generated by MediNest · page X of Y`.
6. Create the export-pdf screen: simple GetX screen with date-range chips, family-member dropdown, three section checkboxes, and a `Generate PDF` button.
7. On generate: show a spinner, call `PdfExportService.generate(...)`, then call `Printing.sharePdf(bytes: bytes, filename: 'medinest-summary-${date}.pdf')` (from `printing` package).
8. Add Premium gate in the entry points (`medicine_history_screen_view.dart`, `appointment_history_screen_view.dart`):
   ```dart
   if (!Preference.shared.getIsPurchase()) {
     Get.bottomSheet(CommonSubscriptionDialog(
       title: 'txtExportPdfPremiumRequired'.tr,
       description: 'txtPaywallBodyMedicines'.tr,  // reuse from F01
       buttonText: 'txtPaywallCtaUpgrade'.tr,
       onTapDelete: () => Get.toNamed(AppRoutes.proVersion),
     ));
     return;
   }
   Get.toNamed(AppRoutes.exportPdf, arguments: { ... });
   ```

## 8. Manual test plan

**Golden path (premium user):**

1. Subscribed user. Open Medicine History.
2. Tap Export PDF.
3. Pick last 30 days, self profile, all sections checked.
4. Tap Generate.
5. **Expected:** PDF generated within 5 seconds (for ~100 entries). System share sheet opens. Save to Files. Open in iOS/Android PDF viewer.
6. Verify content: cover, headers, dates, every dose accounted for.

**Edge case 1 — Free user:**

1. Tap Export PDF.
2. **Expected:** subscription dialog opens. No PDF generated. CTA routes to ProVersion.

**Edge case 2 — Empty range:**

1. Premium user, pick a range with no data (e.g., year 2010).
2. **Expected:** PDF still generates with sections showing "No entries in this period." Don't crash.

**Edge case 3 — Large data:**

1. Premium user with 365 days of dense data, "Last year".
2. **Expected:** PDF generates in <30 seconds, file size <5 MB. If it exceeds, log warning and trim to most recent 365 days.

**Edge case 4 — Special characters in medicine name:**

1. Add medicine `Vitamin D₃ – 1000mg (Mom's pharmacy)` with non-ASCII.
2. Export.
3. **Expected:** renders correctly in PDF. (`pdf` package uses Helvetica by default — use a UTF-8-safe font; embed `assets/fonts/NunitoSans_Regular.ttf` for PDF rendering.)

**Edge case 5 — Arabic / Hindi data:**

1. Premium user, Arabic locale, medicine names in Arabic.
2. Export. **Expected:** RTL text renders correctly. **Note:** the `pdf` package's RTL support requires careful font selection — confirm during implementation.

**Regression check:**

History screens still load and render normally without the export action.

## 9. Rollout

- Internal track first.
- Roll-back: hide the Export PDF button via a `Debug.featurePdfExport` flag. Don't remove the new screen; it's harmless if unreachable.

## 10. Definition of done

Per `../04-definition-of-done.md`. Specifically:

- PDF opens cleanly in Adobe Reader, Apple Preview, and Google Drive
- File name contains date range, no spaces, no special chars
- Premium gate verified

## 11. Out of scope

- Email-the-PDF-direct-to-a-doctor (we use share sheet only — let the OS handle it)
- Cloud-saved exports (Firebase Storage upload) — privacy implications, v2
- Customizable PDF template — v2
- Editable cover page — v2

## 12. Open questions

1. Logo asset — which file is the app's logo for PDF embedding? Verify path in `assets/`.
2. Should journal *photo* attachments (if any) be embedded in the PDF? Recommendation: no in v1 — keeps file size small.
3. Should the PDF include a disclaimer paragraph ("This document is informational only, not medical advice")? Recommend yes — protects against medical-claims policy. Add to footer.

---

## Implemented — STATUS: BLOCKED on package approval

- Date of attempt: 2026-05-09
- versionCode shipped: **none**
- **Why blocked:** the spec explicitly calls out package additions as requiring Abdul's approval before implementation:
  1. `pdf: ^3.x`
  2. `printing: ^5.x`
- Without these, the `PdfExportService` cannot be implemented — the entire feature depends on them.
- **What I did NOT do:**
  - Did not edit `pubspec.yaml`
  - Did not create `PdfExportService` skeleton (it would not compile)
  - Did not create the export-pdf screen
- **What I'm asking from you to unblock F10:**
  1. Confirm both packages can be added.
  2. Confirm the recommendation: ship Premium-gated, with the existing `txtPaywallTitleMedicines` body reused for the "premium feature" prompt (already in F01 keys). No new IAP product needed.
  3. Decide: do we embed `assets/fonts/NunitoSans_Regular.ttf` into the PDF for UTF-8 / Arabic / Hindi support? (Recommend yes — adds ~150 KB to APK but enables global PDF rendering.)
- **Recommended next session:** confirm decision, then I implement service + screen + binding + entry-point gates in one focused pass. Estimate: 4–5 days with packages signed off.
